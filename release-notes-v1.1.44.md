# Pass Vault v1.1.44

## 修复：夜晚模式附件分类筛选下拉白底浅字

### 问题
在深色（夜晚）模式下进入「附件」标签，页面上的「全部附件」分类筛选下拉框仍显示为**白色背景**，其中文字为深色主题设计的浅色，形成**浅字压白底**，几乎不可读。

### 根因
该筛选控件是一个 `<select id="attachment-filter">`。基础样式中它有一条 **ID 选择器**规则硬编码了 `background:#fff` + 浅色边框 `#b8c3cc`。深色主题虽然对 `select` 元素做了统一覆盖，但**元素选择器 `select`（特异性 0,0,1）低于 ID 选择器 `#attachment-filter`（特异性 1,0,1）**，因此深色覆盖不生效，该下拉保留浅色背景。

### 修复
将 `#attachment-filter` 显式加入深色主题的输入表面覆盖清单，使其在夜晚模式下与搜索框一致使用：
- 背景 `var(--input-bg)`（`rgb(17,26,33)`）
- 边框 `var(--input-line)`
- 文字 `var(--input-ink)`（`rgb(242,246,248)`，高对比）

### 验证
- 新增回归红测：夜晚模式下上传附件后，断言 `#attachment-filter` 背景非白、且与 `#search` 搜索框一致 —— 先红后绿。
- 真机视口（390×844, 2x DPR）视觉复核：搜索框与筛选下拉均深色、文字清晰，无其他浅色残留。
- 全量回归通过；CN/EN Changelog 同步。

### 影响范围
纯 CSS 单点修复，仅影响深色主题下 `#attachment-filter` 的背景/边框/文字色，不触及浅色主题、加密、认证或任何数据流。

### 说明
本次与 v1.1.43（详情头部白底修复）为同类「深色覆盖清单遗漏」问题。已借此机会对深色下所有硬编码 `#fff` 元素做审计，目前 `.detail-head` 与 `#attachment-filter` 两处漏网均已补齐。
