# pass-vault-v2 v1.1.40

## 修复
- **弹窗滚动穿透**：在「新建网站 / 编辑账号」等表单里滑动时，只会滚弹窗内容，不再带动背后列表
- **保存按钮可达**：长表单（多账号、分组后）可滚到并看到底部「保存」
- **偶发重复条目**：保存加防重入锁，连点不会重复创建

## 使用
强刷 `pass.23cm.me` / `passkey.23cm.me` 后重试新建/编辑。
