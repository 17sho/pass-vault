# Design archive

This directory contains historical logo explorations and is not part of the runtime or release archives.

- `archive/logo-explorations/` contains superseded comparison sheets in PNG and SVG formats.
- Production icons and branding assets used by the application live under `public/`.

Do not treat files in this directory as the current product logo unless the runtime asset explicitly references them.
