# Release notes archive

Release notes are stored by immutable public version. The current release identity is derived from `package.json`; packaging and documentation checks resolve the current file as:

```text
docs/releases/release-notes-v<VERSION>.md
```

Do not rewrite notes for an already published tag. Publish corrections as a new release or an explicit GitHub Release notice while preserving immutable assets and tags.
