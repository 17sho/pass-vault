import Foundation

// Historical item snapshots and password-protected single-item share files were
// removed from the product. This file intentionally remains as an empty source
// unit so existing Xcode project references continue to build without exposing
// or persisting the retired features.
