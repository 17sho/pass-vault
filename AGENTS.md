# AGENTS.md
只在本仓库工作。先读 `tasks/spec.md`。服务端不得接触明文条目或 vault key。变更后运行 `npm test && npm run lint && npm run typecheck && npm run build`。允许按仓库维护的部署入口发布 Linux/SQLite 与 Cloudflare Worker/D1；生产凭据和资源标识只能从现有环境读取，不得写入代码、文档、提交或日志摘要。