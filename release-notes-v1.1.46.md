# v1.1.46 — 密码生成器 + 空闲自动锁定

本次发布新增两项常用功能,均为纯前端实现,不改动契约与双后端存储。

## ✨ 新增

### 功能4 · 密码生成器
- 账号编辑器每组密码旁新增「生成」按钮,展开面板即可生成强密码。
- 可调长度(12–64,默认 20)与字符集(大写 / 小写 / 数字 / 符号,默认全选)。
- 基于 `crypto.getRandomValues` 无偏拒绝采样,保证每个所选字符集至少一位,随机洗牌后由用户确认填入。
- 默认排除易混字符(0/O、1/l/I);全程浏览器内生成,无任何网络请求。

### 功能2 · 空闲自动锁定
- 登录后启动空闲计时,默认 **5 分钟无操作自动锁库**:清空内存中的 vault key 与解密数据,回到解锁界面。
- 点击 / 键盘 / 触摸 / 指针操作及页面重新可见都会重置计时。
- 与「退出登录」共用清理逻辑;自动锁定不主动注销服务端会话,便于快速重新解锁。
- 手机放桌上离开时的实际防护兜底。

## ✅ 质量
- 新增 UI 集成 E2E:密码生成器长度 / 字符集 / 填入 / 保存;自动锁定超时锁库 + 持续活动不锁库。
- 全量回归、lint、lint:docs、typecheck、build 全部通过。
- 双端部署:Cloudflare Worker(D1)与 Linux(SQLite)两套后端。
